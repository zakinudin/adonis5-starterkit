<!DOCTYPE html>
<html lang="en">
  <head>

    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <!-- Meta -->
    <meta name="description" content="Responsive Bootstrap 4 Dashboard and Admin Template">
    <meta name="author" content="ThemePixels">

    <!-- Favicon -->
    <link rel="shortcut icon" type="image/x-icon" href="/assets/img/favicon.png">

    <title>Cassie Responsive Bootstrap 4 Dashboard and Admin Template</title>

    <!-- vendor css -->
    <link href="/assets/lib/@fortawesome/fontawesome-free/css/all.min.css" rel="stylesheet">
    <link href="/assets/lib/ionicons/css/ionicons.min.css" rel="stylesheet">

    <!-- template css -->
    <link rel="stylesheet" href="/assets/css/cassie.css">

  </head>
  <body>

    <div class="signup-panel">
      <svg-to-inline path="http://themepixels.me/cassie/assets/svg/direction.svg" class-Name="svg-bg"></svg-to-inline>

      <form method="POST" action="/register">
        {{ csrfField() }}
        
        <div class="signin-sidebar">
          <div class="signin-sidebar-body">
            <a href="#" class="sidebar-logo mg-b-40"><span>cassie</span></a>
            <h4 class="signup-title">Create New Account!</h4>
            <h5 class="signup-subtitle">It's free and only takes a minute.</h5>
        
            <div class="signin-form">
              <div class="form-group">
                <label>Username</label>
                <input type="text" name="username" class="form-control {{ flashMessages.has('errors.username') ? 'is-invalid' : '' }}" placeholder="Enter your username" value="">                        
                @if(flashMessages.has('errors.username'))
                  <div class="invalid-feedback">{{ flashMessages.get('errors.username') }}</div>
                @end
              </div>
              <div class="form-group">
                <label>Email address</label>
                <input type="email" name="email" class="form-control {{ flashMessages.has('errors.email') ? 'is-invalid' : '' }}" placeholder="Enter your email address" value="">                        
                @if(flashMessages.has('errors.email'))
                  <div class="invalid-feedback">{{ flashMessages.get('errors.email') }}</div>
                @end
              </div>

              <div class="form-group">
                <label>Password</label>
                <input type="password" name="password" class="form-control {{ flashMessages.has('errors.password') ? 'is-invalid' : '' }}" placeholder="Enter your password" value="">
                @if(flashMessages.has('errors.password'))
                  <div class="invalid-feedback">{{ flashMessages.get('errors.password') }}</div>
                @end
              </div>

              <div class="form-group mg-b-30">
                <div class="custom-control custom-checkbox">
                  <input type="checkbox" name="agreed" class="custom-control-input" id="agree">
                  <label class="custom-control-label tx-sm" for="agree">I have read and agree to your <a href="">Terms of Use</a> and <a href="">Privacy Policy</a></label>
                </div>
              </div>
              <div class="form-group d-flex mg-b-0">
                <button class="btn btn-brand-01 btn-uppercase btn-block">Create New Account</button>
              </div>

            </div>
            <p class="mg-t-auto mg-b-0 tx-color-03">Already have an account? <a href="/login">Signin</a></p>
          </div><!-- signup-sidebar-body -->
        </div><!-- signup-sidebar -->
      </form>
      
    </div><!-- signup-panel -->

    <script src="/assets/lib/jquery/jquery.min.js"></script>
    <script src="/assets/lib/bootstrap/js/bootstrap.bundle.min.js"></script>
    <script src="/assets/lib/feather-icons/feather.min.js"></script>
    <script src="/assets/lib/perfect-scrollbar/perfect-scrollbar.min.js"></script>

    <script>
      $(function(){

        'use strict'

        feather.replace();

        new PerfectScrollbar('.signup-sidebar', {
          suppressScrollX: true
        });

      })
    </script>
    <script src="/assets/js/svg-inline.js"></script>
  </body>
</html>
